/**
 * Classe que representa a entidade de Personagem.
 * Contém propriedades para nome, altura e gênero.
 */
export class Characters {
    /** Nome do personagem. */
    name!: string;
    /** Altura do personagem. */
    height!: string;
    /** Gênero do personagem. */
    gender!: string;    
}